using System.Collections;
using UnityEngine;

public class GameObject : MonoBehaviour
{
    /*
    
    void Start()
    {
        StartCoroutine(RoutineFNC());
    }
    IEnumerator RoutineFNC()
    {
        Debug.Log("Croutine started");
        yield return new WaitForSeconds(2f);
        
        Debug.Log("Worked after 2 seconds");
    } 

    
    public float GecikmeSuresi;
    //play butonuna bastigimizda ilk calisan fonksyondur. Bir kez calisir.
    void Awake()
    {
        print("this massage comes from awake");
    }

    // Nesne aktif oldugu zaman calisir.
    void OnEnable()
    {
        print("object activated");
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("this massage comes from start function");
        Invoke("Yazdir", GecikmeSuresi);
        InvokeRepeating("TekrarliYazdir", 1f, 2f);
    }


    //nesne disable oldugu zamana calisir.
    void OnDisable()
    {
        print("object disebled");
    }
    //update fonksyonu her karede bir kez cagrilir 
    //kullanim amaci kullanicidan imput alma, animasyonlar.
    void Update()
    {
        Debug.Log("function update");

    }
    //fixed update fonksyonu belirli bir zaman araligi ile calisir, varsayilan olarak 0.02 sn dir.
    //kullanim amaci fizik tabanli islemlerde  ve rigidbody ile hareket kuvvet uygulama 
    void FixedUpdate()
    {
        Debug.Log("fixedUpdate function");
    }
    // lateupdate fonksyonu her karenin sonunda calisir . bu metod genellikle diger objeler ustundeki islemler tamamlandiktan sonra calisir.
    //kamera takip haraketleri .
    //Diger objelerin guncellenmesi beklenir.
    void LateUpdate()
    {
        Debug.Log("lateUpdate function");

    }
    //invoke istedigimiz sure gectikten sonra bir kez cagirilir
    //invoke("FonksyonunAdi",gecikme suresi);
    void Yazdir()
    {
        print($"this message show up {GecikmeSuresi} second later");
    }
    void TekrarliYazdir()
    {
        Debug.Log("This message shows up every 2 seconds after 1 second delay.");
    }
    //cancelInvoke fonksyonu ile kapatilabilir.
    



    */
}
// 